## For inital push

```
$ heroku login
$ git init
$ heroku create
$ git add . 
$ git commit -m "initial commit"
$ git push heroku master
```

# For update
```
$ git add . 
$ git commit -m "initial commit"
$ git push heroku master
```

